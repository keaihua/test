// 配置项

const host = 'https://1ctzuwzh.qcloud.la' //开发环境
// const host = 'http://localhost:5757'

const config = {
  host,
  loginUrl: `${host}/weapp/login`,
  userUrl: `${host}/weapp/user`
}

export default config
